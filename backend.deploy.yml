services:
  backend:
    image: reduce:latest
    container_name: reduce-backend
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      DB_PATH: /app/data/reduce.db
      BASE_URL: ${BASE_URL}
    volumes:
      - reduce:/app/data
volumes:
  reduce: